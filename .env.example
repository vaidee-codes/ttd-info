# Secrets must be set in Vercel, never committed here.
DODO_API_KEY=
TTDAF_ENTITLEMENT_PRIVATE_KEY=

# Production uses live Dodo. Preview must use test mode and separate credentials.
DODO_API_BASE=https://live.dodopayments.com
DODO_PRODUCT_ID=pdt_0Nk4Gw67usedtjPoO6hX2
DODO_ENTITLEMENT_ID=ent_0Nk4GugPIsPbnFf5dYYqC

# Both switches fail closed when absent. Enable only after validation.
PASS_SALES_ENABLED=false
PASS_GATE_ENABLED=false

# Read only when VERCEL_ENV=preview. Comma-separated exact origins; no wildcards.
PASS_PREVIEW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
